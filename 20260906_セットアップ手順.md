# セットアップ手順（チーム全員でデータを共有できるようにする）

今のままだと「デモモード」で、自分のブラウザにしかデータが保存されません。
チーム全員で1つのデータを共有できるようにするには、無料の外部サービス
**Supabase（スーパベース）** にデータの保存先を作る必要があります。

Supabaseは「オンラインの表計算シート＋自動で動くサーバー」のようなサービスです。
クレジットカード登録なしの無料プランで、今回の規模（〜10人）なら十分足ります。

所要時間の目安：15〜20分。

---

## 1. Supabaseのアカウントを作る

1. ブラウザで https://supabase.com を開く
2. 右上の「Start your project」または「Sign in」からアカウント登録
   （GitHubアカウントでログインするのが一番早いです。すでにGitHubアカウントをお持ちなので、それでOK）

## 2. 新しいプロジェクトを作る

1. ログイン後、「New Project」をクリック
2. 内容を入力：
   - **Name**：`tfc-record`（好きな名前でOK）
   - **Database Password**：自動生成でも自分で決めてもOK。**必ずどこかにメモしておく**（後で使うことは基本ありませんが念のため）
   - **Region**：`Northeast Asia (Tokyo)` があればそれを選ぶ（一番近くて速い）
3. 「Create new project」をクリックし、1〜2分待つ（サーバーが用意されるのを待つだけ）

## 3. データの入れ物（テーブル）を作る

1. 左側メニューの「SQL Editor」を開く
2. 「New query」をクリック
3. このフォルダにある `schema.sql` ファイルの中身を**全部コピーして貼り付け**
4. 右下（または右上）の「Run」ボタンをクリック
5. 「Success」と表示されればOK（メンバー・記録・大会設定の3つの入れ物ができました）

## 4. 接続情報（合言葉）をコピーする

1. 左側メニューの「Project Settings」（歯車アイコン）→「API」を開く
2. 以下の2つをコピーする：
   - **Project URL**（`https://xxxxx.supabase.co` のような形）
   - **anon public** キー（長い英数字の文字列。「service_role」の方は**絶対に使わない**こと）

## 5. `config.js` を書き換える

1. このフォルダの `config.js` をテキストエディタ（メモ帳やVS Codeなど）で開く
2. 以下の部分を、さっきコピーした値に書き換える：

```js
const SUPABASE_URL = "YOUR_SUPABASE_URL";       // ← Project URL に置き換える
const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY"; // ← anon public キーに置き換える
```

3. 保存する

## 6. 動作確認する

`index.html` をブラウザで開く（ダブルクリックでOK）。
画面上部の「デモモードで動作中」という黄色い帯が消えていれば、Supabaseに正しくつながっています。

ここで自分の名前を登録し、記録を1件入力してみてください。
別のブラウザ（またはシークレットウィンドウ）で同じ `index.html` を開いても同じデータが見えれば成功です。

## 7. チーム全員が使えるように公開する

ここは私（Claude）と一緒にやりましょう。次にお話しするときに「公開して」と言ってください。
今のランニング記録サイトと同じように、GitHubにアップロードして無料の公開URLを発行します
（`index.html` を編集して push すれば、その都度URLはそのまま中身だけ更新される、というのも今と同じです）。

---

## 困ったときは

- **「デモモード」の帯が消えない** → `config.js` の中身が正しくコピーされているか確認（前後の空白やクォートの消し忘れに注意）
- **記録が保存されない／エラーが出る** → SQL Editorで `schema.sql` がちゃんと実行できているか確認（3.の手順をやり直す）
- **anon public キーがどれかわからない** → 「service_role」と書かれた方は使わない。必ず「anon」「public」と書かれた方を使う
